

    //navigator.geolocation.watchPosition(function(position) {
     // let x = document.getElementById("latitude");
//let y = document.getElementById("longitude");

     // navigator.geolocation.getCurrentPosition(showPosition);
    
     //function showPosition(position) {
      //    x.value= position.coords.latitude
      //    y.value = position.coords.longitude 
     // }
   // },
   // function (error) { 
    //  let url_atual = window.location.href;
     // if (error.code == error.PERMISSION_DENIED) {
       // Caso o usuário tenha negado.
      // Swal.fire('Ok, Nós não iremos divulgar sua localização, apenas cidade e estado! Finalize seu cadastro!')
   // }});

 